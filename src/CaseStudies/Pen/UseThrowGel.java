package CaseStudies.Pen;

public class UseThrowGel extends GelPen {

    public UseThrowGel(int size, String name, String brand, int price, PenState penState, OpenAndCloseBehaviour behaviour) {
        super(size, name, brand, price, penState, behaviour);
    }
}
